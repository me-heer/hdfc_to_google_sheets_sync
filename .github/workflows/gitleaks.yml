name: Pylint

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]
    steps:
    - name: Gitleaks
      uses: gitleaks/gitleaks-action@v2.2.0
      env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

